package com.example.primefactors;

import java.util.ArrayList;
import java.util.List;

import org.hibernate.NotImplementedYetException;

/**
 * 💡 Prime Factors Kata
 *
 * 📝 Problem Statement:
 * Given an integer n > 1, return a list of its prime factors.
 * For example:
 * - generate(1) => []
 * - generate(2) => [2]
 * - generate(4) => [2, 2]
 * - generate(6) => [2, 3]
 * - generate(9) => [3, 3]
 *
 * ✅ Goals:
 * - Practice incremental TDD (test one case at a time)
 * - Handle nested loops and basic prime factorization logic
 * - Keep the solution simple and readable (Clean Code)
 */
public class PrimeFactors {

    /**
     * Generates the list of prime factors for a given integer n.
     * For example: 12 -> [2, 2, 3]
     */
    public static List<Integer> generate(int n) {
		throw new UnsupportedOperationException("Not implemented yet");
	}

}
